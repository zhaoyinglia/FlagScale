system:
  no_shared_fs: ${experiment.runner.no_shared_fs}
  num_workers: 2
  tensor_model_parallel_size: 4
  pipeline_model_parallel_size: 4
  expert_model_parallel_size: 8
  decoder_first_pipeline_num_layers: 2 # If use cached dsa_indexer, the first layer in the pp_stage must be `full` not `shared`
  context_parallel_size: 1
  sequence_parallel: true
  use_distributed_optimizer: true
  overlap_grad_reduce: true
  overlap_param_gather: true
  precision:
    bf16: true
    attention_softmax_in_fp32: true
    accumulate_allreduce_grads_in_fp32: true
  logging:
    log_interval: 1
    tensorboard_log_interval: 1
    wandb_project: ${experiment.exp_name}
    wandb_exp_name: ${experiment.exp_name}
    log_timers_to_tensorboard: true
    log_validation_ppl_to_tensorboard: true
    log_throughput: true
    log_params_norm: true
    log_num_zeros_in_grad: true
    log_memory_to_tensorboard: true
  checkpoint:
    save_interval: ${experiment.save_steps}
    load: ${experiment.load}
    ckpt_format: ${experiment.ckpt_format}

model:
  transformer_impl: transformer_engine
  num_layers: 78
  hidden_size: 6144
  num_attention_heads: 64
  num_query_groups: 64
  seq_length: 4096
  max_position_embeddings: 4096
  norm_epsilon: 1e-5
  use_rotary_position_embeddings: true
  rotary_base: 1000000
  no_rope_fusion: true
  swiglu: true
  normalization: RMSNorm
  qk_layernorm: true
  init_method_std: 0.02
  attention_dropout: 0.0
  hidden_dropout: 0.0
  position_embedding_type: rope
  untie_embeddings_and_output_weights: true
  no_position_embedding: true
  disable_bias_linear: true

  # MLA args
  multi_latent_attention: true
  q_lora_rank: 2048
  kv_lora_rank: 512
  qk_head_dim: 192
  qk_pos_emb_head_dim: 64
  v_head_dim: 256

  # DSA args (all layers use DSA)
  experimental_attention_variant: dsa
  dsa_indexer_n_heads: 32
  dsa_indexer_head_dim: 128
  dsa_indexer_topk: 2048
  dsa_indexer_loss_coeff: 0.01
  indexer_type_rule: "2*full+repeat(full, shared, shared, shared)" # GLM5/GLM5.1 is None, GLM5.2 is "2*full+repeat(full, shared, shared, shared)"

  # MoE args
  ffn_hidden_size: 12288
  moe_ffn_hidden_size: 2048
  moe_grouped_gemm: true
  moe_shared_expert_intermediate_size: 2048
  num_experts: 256
  moe_router_load_balancing_type: "none"
  moe_router_score_function: sigmoid
  moe_router_enable_expert_bias: true
  moe_router_bias_update_rate: 0.001
  moe_aux_loss_coeff: 0.0
  moe_layer_freq: "[0]*3+[1]*75"
  moe_router_num_groups: 1
  moe_router_group_topk: 1
  moe_router_topk: 8
  moe_router_topk_scaling_factor: 2.5
  moe_token_dispatcher_type: "alltoall"

  # MTP args
  mtp_num_layers: 0 # The origin config is 1, but the released checkpoint has no MTP layer
  mtp_loss_scaling_factor: 0.3

  # Training
  seed: ${experiment.seed}
  finetune: false
  micro_batch_size: 1
  global_batch_size: 2048
  eval_iters: 0
  eval_interval: 1000
  train_iters: 102400

  optimizer:
    clip_grad: 1.0
    weight_decay: 0.1
    adam_beta1: 0.9
    adam_beta2: 0.95
    lr_scheduler:
      lr: 3.0e-4
      min_lr: 3.0e-5
      lr_warmup_fraction: 0.01
      lr_decay_style: WSD
      lr_wsd_decay_style: cosine
      lr_wsd_decay_iters: 10

data:
  reset_position_ids: true
  reset_attention_mask: true
  data_path: /share/project/lizhiyu/data/pile_wikipedia_demo
  split: 1
  no_mmap_bin_files: true
  tokenizer:
    tokenizer_type: HuggingFaceTokenizer
    tokenizer_model: /share/project/lizhiyu/data/GLM-5/
    vocab_size: 154880
    make_vocab_size_divisible_by: 64
